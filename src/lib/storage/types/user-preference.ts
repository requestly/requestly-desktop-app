export interface UserPreferenceObj {
  defaultPort: number;
  isEnabled: boolean;
  storePath: string;
  filter: string[]
}

export interface ISource {
  defaultPort: number;
  isEnabled: boolean;
  storePath: string;
  filter: string[]
}
